<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">
	<!-- 회원 가입-->
	<insert id="signup" parameterType="userinfodto"> 
		INSERT INTO USER_INFO VALUES (user_index_seq.nextval, #{user_id }, #{user_pw }, #{user_name }, sysdate, #{user_tell}, #{user_email})
	</insert>
	
	<!-- 로그인 -->
	<select id="login" parameterType="hashmap" resultType="_int">
		SELECT COUNT(*) FROM USER_INFO WHERE USER_ID = #{user_id} AND USER_PW = #{user_pw}
	</select>
	
</mapper>